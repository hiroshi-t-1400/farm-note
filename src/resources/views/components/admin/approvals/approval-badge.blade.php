@if ($pendingCount > 0)
    <span class="inline-flex item-center justify-center px-2 py-0.5 ml-2 text-xs font-bold leading-none text-red-100 bg-red-600 rounded-full">
        {{ $pendingCount }}
    </span>
@endif
